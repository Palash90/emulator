    /*
* Not CHIP
*/
import Nor.hdl

CHIP Not {
    IN a[26];
    OUT                   out;

    PARTS          :
     
     Nor     (a=a, b=a, out=out); // If we short two inputs of nor gate, we essentially get a not gate.                                                                                         
}
