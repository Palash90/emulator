const Token = require("../../parser/Token")

const result = {
    error: true,
    errorMessage:"PARTS_BLANK.hdl:Expected CHIP Name, but got '}' at line:10 column:1"
}

module.exports = {
    result
}