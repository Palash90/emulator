const Token = require("../../parser/Token")

const result = {
    error: true,
    errorMessage:"CHIP_CALL_VAID_PARAM.hdl:Expected Variable, but got '}' at line:11 column:1"
}

module.exports = {
    result
}