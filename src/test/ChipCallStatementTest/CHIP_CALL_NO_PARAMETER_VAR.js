const Token = require("../../parser/Token")

const result = {
    error: true,
    errorMessage:"CHIP_CALL_NO_PARAMETER_VAR.hdl:Expected CHIP Name, but got 'Nor' at line:10"
}

module.exports = {
    result
}