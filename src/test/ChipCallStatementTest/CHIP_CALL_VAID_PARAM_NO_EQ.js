const Token = require("../../parser/Token")

const result = {
    error: true,
    errorMessage:"CHIP_CALL_VAID_PARAM_NO_EQ.hdl:Expected Chip Parameter, but got 'a' at line:10 column:8"
}

module.exports = {
    result
}