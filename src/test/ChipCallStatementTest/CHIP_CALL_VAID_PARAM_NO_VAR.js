const Token = require("../../parser/Token")

const result = {
    error: true,
    errorMessage:"CHIP_CALL_VAID_PARAM_NO_VAR.hdl:Expected Variable, but got 'IN' at line:10 column:11"
}

module.exports = {
    result
}