const Token = require("../../parser/Token")

const result = {
    error: true,
    errorMessage:"CHIP_CALL_VAID_PARAM_NO_VAR.hdl:Expected Chip Parameter, but got 'a' at line:10 column:11"
}

module.exports = {
    result
}