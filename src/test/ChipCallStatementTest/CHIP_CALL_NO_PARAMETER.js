const Token = require("../../parser/Token")

const result = {
    error: true,
    errorMessage:"CHIP_CALL_NO_PARAMETER.hdl:Expected Chip Parameter, but got 'IN' at line:10 column:9"
}

module.exports = {
    result
}