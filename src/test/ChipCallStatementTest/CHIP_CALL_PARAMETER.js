const Token = require("../../parser/Token")

const result = {
    error: true,
    errorMessage:"CHIP_CALL_PARAMETER.hdl:Expected CHIP Name, but got 'Nor' at line:10 column:1"
}

module.exports = {
    result
}