const Token = require("../../parser/Token")

const result = {
    error: true,
    errorMessage:"INVALID_PARTS.hdl:Expected CHIP Name, but got 'IN' at line:10 column:1"
}

module.exports = {
    result
}